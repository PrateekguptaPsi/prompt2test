# Odoo target (cancer-center HMS) — fill locally, never commit real values
ODOO_URL=
ODOO_DB=
ODOO_USER=
ODOO_PASSWORD=

# Jira (bug filing) — generate a fresh API token; never reuse a leaked one
JIRA_BASE_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=
JIRA_PROJECT_KEY=
